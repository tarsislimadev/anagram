version: '3'

services:
  www:
    build: './src/www'

  docker-push:
    image: 'tmvdl/projects:anagram'
    build: './src'
    ports:
      - '80:80'

  github-release:
    image: 'github-release'
    build: './src/www'
